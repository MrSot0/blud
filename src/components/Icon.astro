---
import { iconSvg, type IconName } from '../lib/icons';

interface Props {
  name: IconName;
  class?: string;
}

const { name, class: className = 'size-5' } = Astro.props;
---

<Fragment set:html={iconSvg(name, className)} />
